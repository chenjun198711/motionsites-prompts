# 手机安装 MotionSites Gallery APK 教程

> 零基础也能看懂。全程在手机上操作，不需要电脑。

## 这是什么？

这是一个 AI 设计提示词浏览器应用，包含 328 个 MotionSites 提示词，支持搜索、筛选、一键复制和详情预览。

## 第一步：触发 GitHub 自动打包

1. 打开手机浏览器，访问这个仓库：
   **https://github.com/xianxian-sensen/motionsites-prompts**

2. 点击页面上方的 **Actions** 标签（在 `Code`、`Issues` 旁边）。

3. 左侧列表里点击 **Build Android APK**。

4. 右侧会显示一个灰色的提示框，写着 "This workflow has no runs yet" 或类似文字。
   点击上方的 **Run workflow** 按钮（绿色/蓝色按钮）。

5. 弹出的下拉框里再点一次 **Run workflow**。

6. 等待 3-5 分钟，页面会自动刷新，出现一个黄色圆点表示正在运行。
   等黄色圆点变成绿色对勾 ✅，就表示打包完成了。

## 第二步：下载 APK

1. 点击刚完成的那个 workflow 运行记录（绿色对勾那一行）。

2. 页面最下方有一个 **Artifacts** 区域。

3. 找到 **app-debug-apk**，点击它下载。

4. 下载完成后，浏览器通知栏或下载记录里会多一个 `app-debug-apk.zip` 文件（GitHub 会自动压缩）。

5. 用手机自带的文件管理器找到这个 zip，解压后得到 `app-debug.apk`。

## 第三步：安装 APK

1. 点击 `app-debug.apk`。

2. 手机可能会提示 "未知来源应用" 或 "禁止安装"。
   点击提示里的 **设置** → 允许 "此来源" 或 "安装未知应用"，然后返回重新点击安装。

3. 安装完成后，桌面上会出现一个名为 **MotionSites Gallery** 的图标。

4. 打开即可使用。

## 常见问题

**Q：为什么提示"无法安装"？**
A：可能是下载不完整。回到 GitHub Actions 重新下载一次。

**Q：应用打不开或闪退？**
A：可能是手机系统版本过低。本应用需要 Android 5.0 以上。

**Q：提示词打不开？**
A：所有提示词已经内嵌到应用里，不需要联网。如果打不开，尝试清除应用数据后重新打开。

**Q：我想更新到最新版本？**
A：重新执行第一步，GitHub 会重新打包最新代码，下载安装即可。
