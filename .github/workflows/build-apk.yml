name: Build Android APK

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Install dependencies
        working-directory: motionsites-app
        run: npm ci

      - name: Sync Capacitor
        working-directory: motionsites-app
        run: npx cap sync android

      - name: Build debug APK
        working-directory: motionsites-app/android
        run: ./gradlew assembleDebug

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: app-debug-apk
          path: motionsites-app/android/app/build/outputs/apk/debug/app-debug.apk
          retention-days: 30